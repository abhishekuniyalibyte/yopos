[pytest]
# Put the project root on sys.path so `pytest` resolves the `server` package from
# anywhere, without needing an editable install or PYTHONPATH.
pythonpath = .
testpaths = tests
asyncio_default_fixture_loop_scope = function
